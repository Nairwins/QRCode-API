{
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "dnf install -y cairo cairo-devel && pip install -r requirements.txt"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}